const {MessageEmbed} = require('discord.js');

module.exports = {
    name: "server",
    description: "Show Server Info",
    category: "info",
    usage: "[none]",
    aliases: ["infoserver"],
    run: async(bot,message,args) => {
        
    },
};